# change config path
export ZDOTDIR="$HOME/.config/zsh"

# change vim config path
export VIMINIT="source $HOME/.config/vim/.vimrc"
export MYVIMRC="$HOME/.config/vim/.vimrc"

# rust binaries 
export PATH="$PATH:$HOME/.cargo/bin"

# local binaries
export PATH="$PATH:$HOME/.local/bin"

# fucking ruby
# why the fuck does every new language drop their stuff in a new local folder
# you're not hip you fucking idiots
# this is byfar the worst
# even version numbers??? Ya'll braindead monkeys every heard of package managers ?
# clowns
export PATH="$PATH:$HOME/.gem/ruby/2.7.0/bin"

